<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">127.0.0.1</domain> <!-- For physical devices testing localhost -->
        <domain includeSubdomains="true">10.0.2.2</domain> <!-- For emulators -->
        <!-- Add your development machine's local IP if testing on a physical device connected to the same network -->
        <!-- <domain includeSubdomains="true">192.168.1.5</domain> -->
    </domain-config>
</network-security-config>
        